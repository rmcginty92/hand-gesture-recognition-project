cmake_minimum_required(VERSION 2.8)
project( haarClassifierTest )
find_package( OpenCV REQUIRED )
set(INCLUDE_DIR "usr/include/opencv2")
include_directories (${INCLUDE_DIR})
add_executable( haarClassifierTest testHaarClassifier.cpp )
target_link_libraries( haarClassifierTest ${OpenCV_LIBS} )
